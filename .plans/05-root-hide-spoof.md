# 05 Root + hide + spoof — BASIC+DEVICE gaming only

Refs: `.research/04-root-hide-spoof-2026.md:9-63`; `.devdocs/PlayIntegrityFork/{module.prop:v18/180001,update.json:180000,CHANGELOG.md:v18 ShadowHook/autopif Beta,module/{autopif4.sh:279,post-fs-data.sh,service.sh,common_func.sh,common_setup.sh,customize.sh,action.sh,killpi.sh,migrate.sh,app_replace_list.txt,example.pif.prop},app/.../cpp/main.cpp:Zygisk+ShadowHook,app/.../EntryPoint.java}`; `.devdocs/TrickyStore/{update.json:v1.4.1/245,module.json,changelog.md}`; `.devdocs/Tricky-Addon/module/{post-fs-data.sh,service.sh,prop.sh,get_extra.sh,customize.sh},module.prop:v5.0-beta.4/729 vs update v4.4`; `.devdocs/Vector/zygisk/.../ParasiticManagerHooker.kt,module.prop:v2.2/3080`; `.devdocs/NeoZygisk/loader/src/ptracer/*,module:v2.4/289,zygiskd:1.1.0`; `.devdocs/HideMyApplist-mirror/README.md:SOURCE Dr-TSNG/Hide-My-Applist`; `.devdocs/DeviceSpoofLab-Hooks/{device_profile.conf:260,app/.../MainHook.java,hooks/{BuildHooks,TelephonyHooks,NetworkHooks:30-148,+19 files},cpp/{native_hooks,property_hooks,system_hooks,dlopen_hook}.cpp,module.prop:1.2/4 targetApi101,xposed api:82}`; `.devdocs/TikTokSimSpoof/.../{MainHook.kt,config/SpoofConfig.kt:60+ MCC/MNC},1.2.0/2`; `.devdocs/TelephonySpoofer/.../SpoofInjection.java:hookBool/String/Int,1.0/1`; `.devdocs/Lsposed-SimSpoof/.../SimSpoofModule.java:750 scope android hardcoded DISCARD`; `.devdocs/waydroid_network_spoof/README.md:hasTransport/type table,APKs WaydroidNetworkSpoof+Test`; `.devdocs/KeyAttestation/...:1.8.4,status.json`.

## Goal
Per-game BASIC+DEVICE. No STRONG promise. No shipped keybox/prints. Start minimal, escalate on kick.

## Layer order (implement exactly)
1. Base props via 04 templates (Pixel print current, NOT 2023 redfin). release-keys/user, `debuggable=0 secure=1 adb.secure=1 selinux=enforcing`.
2. ARM bridge libndk/houdini via sidecar (`native.bridge`, abilist, binfmt).
3. Magisk v30.7+/v31 bootanim.rc inject (04). DenyList per PKG. Enforce OFF with Shamiko.
4. Shamiko 1.2.5 LSPosed.io prebuilt closed. Blacklist default, whitelist `/data/adb/shamiko/whitelist`.
5. PIFork osm0sis v18 canonical: reuse `common_func.sh:delprop/resetprop_if_diff/resetprop_hexpatch`, `post-fs-data.sh` early props (warranty 0, tags release-keys, type user, adb.secure 1, debuggable 0, secure 1), `service.sh` late (boot.mode unknown, selinux enforcing, secureboot locked, flash locked, verifiedboot green, verity enforcing, oem_unlock 0, `resetprop --compact`), `common_setup.sh` USNF remove + `.replace` whiteout, `autopif4.sh:279` Canary generator (flash JSON + factory headers + bulletin SECURITY_PATCH → MANUFACTURER/Google/MODEL/FINGERPRINT CANARY/PRODUCT/DEVICE/PATCH/INITIAL_SDK 32 → `migrate.sh -i`, preserve spoof*/verbose flags, update TrickyStore patch), `action.sh:autopif4 -m`, `killpi.sh:killall gms.unstable/vending`. No bundled print; user private print; public dies days, canary ~6wk. Native `main.cpp` ShadowHook `__system_property_read_callback` — do NOT fork.
6. TrickyStore v1.4.1 + Addon v4.4 OPT-IN empty slot only. Merge Addon `prop.sh` late list (vbmeta locked, verifiedboot green, flash locked, verity enforcing, warranty 0, debuggable 0, secure 1, type user, tags release-keys, recovery unknown, digests) with PIF service.sh, dedupe. AOSP box = DEVICE max. RKP RSA-4096 Feb-Apr 2026 kills old 2048. Verify with KeyAttestation 1.8.4 (offline, `status.json`).
7. Vector v2.2/3080 + NeoZygisk v2.4/289 (NeoZygisk ONLY if Magisk Zygisk missing on Waydroid; parasitic hooker overkill — skip unless manager needed). HMA source `Dr-TSNG/Hide-My-Applist` v3.8.3 API101 ONLY if applist kick; SimSpoof already ports PM filter, mirror is mirror-only.
- Escalation: built-in Zygisk+Shamiko+PIFork first. ZygiskNext v1.5.0 (closed v4-0.9.2+, fetch zip — git clone fails) or ReZygisk v1.0.0 open APIv4 parity ONLY on detection hit. Closed slows YARA, Google detects behavior (mounts/maps/syscalls) — value is Linker+SoInfo+umount, not secrecy.

## Own modules (ship; Frida lab-only)
- Frida no-ship: detectable `/proc/self/maps` frida/linjector, TCP 27042, no persist, Gadget repack breaks sig. Lab recon/objection only.
- Base = DeviceSpoofLab-Hooks 1.2/4 whole module: `MainHook.java` (attach-defer, BuildHooks first, skip own pkg, 2s generation poll), `BuildHooks` (BOARD..SERIAL, VERSION.*, getSerial/getRadioVersion/Partition), `TelephonyHooks` (Imei/Meid/Subscriber/SimSerial/Line1 int-variants, Operator/SimOperator, SubscriptionManager.getPhoneNumber), +19 hooks files, `NetworkHooks.java:30-148` (WifiInfo MAC/BSSID/SSID, ScanResults, BT addr/name, NetworkInterface hwaddr), native `native/property/system/dlopen` + `NativeHooks.tryInstall`, xposed api 82 / service 101 / min 100. Feed `device_profile.conf`.
- `modules/network-shim`: PORT net table (README-only, no code): `hasTransport ETHERNET true→false, WIFI+CELL false→true; NetworkInfo.getType 9→1, TypeName Ethernet→WIFI; WifiManager enabled false→true, state 1→3; Telephony NetworkType 0→13 LTE, DataState→2; ActiveNetworkInfo.mType 9→1; eth0→wlan0; WiFi wins`. Targets `hasTransport/getType/getActiveNetworkInfo/getWifiState/isWifiEnabled/getNetworkType/DataState/getNetworkInterfaces` via WeakHashMap+XposedBridge.hookAllMethods. Limits: restart required, registerNetworkCallback still real, DNS eth0, SysUI untouched. Test with companion Test APK scoped separately.
- `modules/telephony-shim`: fork TikTok `MainHook.kt` scope→games (NOT tiktok-only) + `SpoofConfig.kt` 60+ MCC/MNC table (defaults us 31026 T-Mobile LTE13) + TelephonySpoofer `hookBool/String/Int` XSharedPrefs runtime-edit pattern. Discard Lsposed-SimSpoof hardcoded ICCID/IMSI/IMEI + framework-only scope idea kept, values NOT.
- `modules/sensor-noise`: BUILD (no maintained module exists): PriviSense `dispatchSensorEvent` hook + OU noise alpha 0.05 4m clamp. Variance test: noise floor 0.01-0.05 m/s² else fail.
- Root toggle: enable = ensure bootanim block + restart; disable = restore original + purge overlay magisk* + clear `/data/adb/magisk.db`. Prefer per-app DenyList+Shamiko+HMA over global off.

## Guardrails
Verify YASNAC/Integrity Checker/Applist Detector + KeyAttestation. Never gate banking PKGs. Warn cheats. Never bundle keybox/prints — runtime fetch + auto-update. Cat-mouse ~6wk.

## Blast radius
- Touches: `modules/*`, `crates/wd-spoof/*`, Magisk overlay only. No engine/UI/MCP.
- Risk: HIGH (bootloop/ban/revoke). Guard: per-profile sandbox, clear gms+game after swap, updater for prints, confirm flags.
- Rollback: toggle disable → restore props → uninstall modules → restart.

# Per-game spoof profile (cheetah canonical).
# Refs: `.devdocs/DeviceSpoofLab-Hooks/device_profile.conf:260`
# (Pixel 7 Pro cheetah A16 SDK36), `.research/04-root-hide-spoof-2026.md:40`
# (redfin print dead, verified-boot list only). NOT secrets (skill waydroid-spoof).
fingerprint = "google/cheetah/cheetah:16/BP4A.250605.009/12621605:user/release-keys"
fake_touch = ["com.example.game*"]
fake_wifi = ["com.example.game*"]
denylist = ["com.example.game"]
